(function () {
    'use strict';

    angular
        .module('os2')
        .run(appRun);

    appRun.$inject = ['$location'];

    /* @ngInject */
    function appRun($location) {
        
    }
})();
