(function () {
    'use strict';

    angular
        .module('app.core')
        .run(appRun);

    appRun.$inject = ['$location'];

    /* @ngInject */
    function appRun($location) {
        
    }
})();
